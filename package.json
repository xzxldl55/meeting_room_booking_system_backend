{
  "name": "meeting_room_booking_system_backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "pnpm -r --filter=server run start:dev",
    "admin": "pnpm -r --filter=frontend-admin run start",
    "user": "pnpm -r --filter=frontend-user run start",
    "ui": "pnpm run admin && pnpm run user"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "dayjs": "^1.11.11"
  }
}